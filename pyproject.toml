[project]
name = "TreeBuilder Beta"
version = "1.0.0"
description = "A Python app to create family trees."
requires-python = ">=3.11"
dependencies = [
    "networkx>=3.5",
    "pandas>=2.3.2",
    "plotly>=6.3.0",
    "streamlit>=1.49.1",
]